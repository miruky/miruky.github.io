import type { LangCourse } from './types';

const course: LangCourse = {
  id: 'c-lang',
  name: 'C',
  nameJa: 'C言語',
  simpleIconSlug: 'c',
  color: '#A8B9CC',
  description: 'システムプログラミングの基盤。メモリ管理やポインタを深く理解できます。',
  lessons: [
    {
      id: 1,
      title: 'はじめてのCプログラム',
      difficulty: 'beginner',
      description: 'C言語は1972年にDennis Ritchieが開発したシステムプログラミング言語です。OS、組み込みシステム、コンパイラなど低レベルな開発に広く使われています。',
      task: 'printf で "Hello, C!" と表示してください。',
      initialCode: '#include <stdio.h>\n\nint main(void) {\n    // ここに printf を書く\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\nint main(void) {\n    printf("Hello, C!\\n");\n    return 0;\n}',
      expectedOutput: 'Hello, C!',
      hints: ['printf("テキスト\\n"); で出力', '#include <stdio.h> は標準入出力ヘッダ'],
      explanation: '#include はプリプロセッサ指令でヘッダファイルを取り込みます。main() はプログラムのエントリポイントで、return 0; は正常終了を意味します。\\n は改行文字です。',
    },
    {
      id: 2,
      title: '変数とデータ型',
      difficulty: 'beginner',
      description: 'C言語の基本データ型: int(整数), float/double(浮動小数点), char(文字)。変数は使用前に型を宣言する必要があります。',
      task: 'int 型の変数 age に 25 を代入し、printf で表示してください。',
      initialCode: '#include <stdio.h>\n\nint main(void) {\n    // int 型の変数を宣言して表示\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\nint main(void) {\n    int age = 25;\n    printf("Age: %d\\n", age);\n    return 0;\n}',
      expectedOutput: 'Age: 25',
      hints: ['%d は int の書式指定子', 'int age = 25; で宣言と初期化'],
      explanation: '書式指定子: %d(int), %f(float/double), %c(char), %s(文字列), %p(ポインタ), %ld(long), %u(unsigned)。sizeof演算子で型のバイト数を確認できます。int は通常4バイト(32bit)です。',
    },
    {
      id: 3,
      title: '算術演算子と型変換',
      difficulty: 'beginner',
      description: '四則演算 +, -, *, / と剰余 % 。整数同士の除算は小数部が切り捨てられるので注意が必要です。',
      task: '10 / 3 の整数除算結果と、キャストによる浮動小数点除算結果を表示してください。',
      initialCode: '#include <stdio.h>\n\nint main(void) {\n    int a = 10, b = 3;\n    // 整数除算と浮動小数点除算\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\nint main(void) {\n    int a = 10, b = 3;\n    printf("Integer: %d\\n", a / b);\n    printf("Float: %.2f\\n", (double)a / b);\n    return 0;\n}',
      expectedOutput: 'Integer: 3\nFloat: 3.33',
      hints: ['(double)a でキャスト', '%.2f は小数点以下2桁'],
      explanation: '整数同士の除算 10/3 = 3（切り捨て）。一方をdoubleにキャスト (double)10/3 = 3.33...。暗黙の型変換（型昇格）: int + double → double に自動変換されます。',
    },
    {
      id: 4,
      title: '条件分岐 (if / else)',
      difficulty: 'beginner',
      description: 'if-else 文で条件に応じた処理を分岐します。比較演算子(<, >, ==, !=, <=, >=)と論理演算子(&&, ||, !)を使います。',
      task: '変数 score が 60 以上なら "Pass"、未満なら "Fail" と表示してください。',
      initialCode: '#include <stdio.h>\n\nint main(void) {\n    int score = 75;\n    // if-else で合否判定\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\nint main(void) {\n    int score = 75;\n    if (score >= 60) {\n        printf("Pass\\n");\n    } else {\n        printf("Fail\\n");\n    }\n    return 0;\n}',
      expectedOutput: 'Pass',
      hints: ['if (score >= 60) { ... }', '== は比較、= は代入（混同注意）'],
      explanation: '三項演算子: result = (score >= 60) ? "Pass" : "Fail"; で短く書けます。switch 文は定数値の多分岐に適しています。= と == の混同はC言語の典型的なバグです。',
    },
    {
      id: 5,
      title: 'ループ (for / while)',
      difficulty: 'beginner',
      description: 'for は回数が決まった繰り返し、while は条件ベースの繰り返しに使います。break と continue でループを制御します。',
      task: 'for ループで 1 から 5 まで表示してください。',
      initialCode: '#include <stdio.h>\n\nint main(void) {\n    // for ループ\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\nint main(void) {\n    for (int i = 1; i <= 5; i++) {\n        printf("%d\\n", i);\n    }\n    return 0;\n}',
      expectedOutput: '1\n2\n3\n4\n5',
      hints: ['for (int i = 1; i <= 5; i++)', 'i++ は i = i + 1 と同じ'],
      explanation: 'for (初期化; 条件; 更新) { 本体 }。do-while は少なくとも1回実行されます。無限ループ: for(;;) または while(1)。C99以降、forのなかで変数宣言が可能です。',
    },
    {
      id: 6,
      title: '配列',
      difficulty: 'beginner',
      description: '同じ型のデータを連続したメモリに格納します。配列のインデックスは0から始まります。',
      task: 'int 型配列に {10, 20, 30, 40, 50} を格納し、全要素をループで表示してください。',
      initialCode: '#include <stdio.h>\n\nint main(void) {\n    // 配列の宣言と表示\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\nint main(void) {\n    int nums[] = {10, 20, 30, 40, 50};\n    int len = sizeof(nums) / sizeof(nums[0]);\n    for (int i = 0; i < len; i++) {\n        printf("%d\\n", nums[i]);\n    }\n    return 0;\n}',
      expectedOutput: '10\n20\n30\n40\n50',
      hints: ['int nums[] = {10, 20, 30, 40, 50};', 'sizeof(nums)/sizeof(nums[0]) で要素数'],
      explanation: 'C言語の配列は長さ情報を持ちません。sizeof で全体のバイト数 / 1要素のバイト数で要素数を計算します。配列の境界チェックはないため、out-of-bounds アクセスはバッファオーバーフロー脆弱性の原因になります。',
    },
    {
      id: 7,
      title: '文字列 (char 配列)',
      difficulty: 'beginner',
      description: 'C言語に文字列型はなく、char の配列で表現します。末尾に null 文字 (\\0) が必要です。',
      task: 'char配列で "Hello" を定義し、1文字ずつ表示してください。',
      initialCode: '#include <stdio.h>\n#include <string.h>\n\nint main(void) {\n    // char配列で文字列\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n#include <string.h>\n\nint main(void) {\n    char greeting[] = "Hello";\n    int len = strlen(greeting);\n    for (int i = 0; i < len; i++) {\n        printf("%c\\n", greeting[i]);\n    }\n    return 0;\n}',
      expectedOutput: 'H\ne\nl\nl\no',
      hints: ['char greeting[] = "Hello"; は自動で \\0 が付く', 'strlen() で文字列の長さを取得'],
      explanation: '"Hello" は {H,e,l,l,o,\\0} と等価で6バイト必要です。string.h の関数: strlen(長さ), strcpy(コピー), strcat(連結), strcmp(比較)。バッファサイズを超える操作は脆弱性の原因になります。strncpy 等の安全な関数を使いましょう。',
    },
    {
      id: 8,
      title: '関数',
      difficulty: 'beginner',
      description: '関数で処理をまとめ再利用します。戻り値の型、関数名、引数リストを宣言します。プロトタイプ宣言を使うと順序を気にせず呼び出せます。',
      task: '2つの int を受け取り、大きい方を返す max 関数を作ってください。',
      initialCode: '#include <stdio.h>\n\n// max 関数を定義\n\nint main(void) {\n    printf("Max: %d\\n", max(10, 25));\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\nint max(int a, int b) {\n    return (a > b) ? a : b;\n}\n\nint main(void) {\n    printf("Max: %d\\n", max(10, 25));\n    return 0;\n}',
      expectedOutput: 'Max: 25',
      hints: ['int max(int a, int b) { return ...; }', '三項演算子で簡潔に書ける'],
      explanation: 'C言語の引数は「値渡し」(pass by value)です。関数内で引数を変更しても呼び出し元には影響しません。ポインタを渡せば「参照渡し」のように動作します。再帰関数も可能ですが、スタックオーバーフローに注意が必要です。',
    },
    {
      id: 9,
      title: 'ポインタ入門',
      difficulty: 'beginner',
      description: 'ポインタはメモリアドレスを格納する変数です。& でアドレスを取得し、* でアドレス先の値を参照（間接参照）します。',
      task: 'int 型変数のポインタを作り、ポインタ経由で値を変更してください。',
      initialCode: '#include <stdio.h>\n\nint main(void) {\n    int x = 10;\n    // ポインタを作り、値を変更\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\nint main(void) {\n    int x = 10;\n    int *p = &x;\n    *p = 42;\n    printf("x = %d\\n", x);\n    return 0;\n}',
      expectedOutput: 'x = 42',
      hints: ['int *p = &x; でアドレスを格納', '*p = 42; で間接参照で値を変更'],
      explanation: 'ポインタはC言語の最重要概念です。&x: x のアドレス、*p: p が指す先の値。ポインタにより関数から複数の値を返す、動的メモリ管理、データ構造の構築が可能になります。NULL ポインタの間接参照はセグフォの原因です。',
    },
    {
      id: 10,
      title: '構造体 (struct)',
      difficulty: 'beginner',
      description: '構造体は異なる型のデータをまとめる複合データ型です。オブジェクト指向のクラスの前身と言えます。',
      task: 'name と age を持つ Person 構造体を定義し、値を表示してください。',
      initialCode: '#include <stdio.h>\n\n// Person 構造体を定義\n\nint main(void) {\n    // Person を作成し表示\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\ntypedef struct {\n    char name[50];\n    int age;\n} Person;\n\nint main(void) {\n    Person p = {"Alice", 30};\n    printf("Name: %s, Age: %d\\n", p.name, p.age);\n    return 0;\n}',
      expectedOutput: 'Name: Alice, Age: 30',
      hints: ['typedef struct { ... } Person;', 'p.name でメンバアクセス'],
      explanation: 'typedef で型の別名を定義できます。構造体ポインタでは -> 演算子: ptr->name。構造体は値渡しなので、大きな構造体はポインタで渡すのが効率的です。パディングによるメモリアラインメントにも注意が必要です。',
    },
    // ── Intermediate (11-20) ──
    {
      id: 11,
      title: 'ポインタと配列',
      difficulty: 'intermediate',
      description: '配列名は先頭要素へのポインタとして扱われます。ポインタ演算で配列を操作できます。',
      task: 'ポインタ演算で配列の全要素を表示してください。',
      initialCode: '#include <stdio.h>\n\nint main(void) {\n    int arr[] = {10, 20, 30, 40, 50};\n    int *p = arr;\n    // ポインタ演算で全要素を表示\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\nint main(void) {\n    int arr[] = {10, 20, 30, 40, 50};\n    int *p = arr;\n    for (int i = 0; i < 5; i++) {\n        printf("%d\\n", *(p + i));\n    }\n    return 0;\n}',
      expectedOutput: '10\n20\n30\n40\n50',
      hints: ['*(p + i) は p[i] と同じ', '配列名は先頭要素のポインタ'],
      explanation: 'arr[i] は *(arr + i) の糖衣構文です。ポインタ演算は型のサイズ分進みます（int*なら4バイト単位）。sizeof(arr) は配列全体のサイズ、sizeof(p) はポインタのサイズ（8バイト/64bit）です。',
    },
    {
      id: 12,
      title: '動的メモリ管理 (malloc/free)',
      difficulty: 'intermediate',
      description: 'malloc() でヒープメモリを確保し、free() で解放します。メモリリークを防ぐため確保したメモリは必ず解放します。',
      task: 'malloc で5要素の int 配列を作り、値を設定して表示し、free で解放してください。',
      initialCode: '#include <stdio.h>\n#include <stdlib.h>\n\nint main(void) {\n    // malloc で配列を動的確保\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n#include <stdlib.h>\n\nint main(void) {\n    int *arr = (int *)malloc(5 * sizeof(int));\n    if (arr == NULL) {\n        printf("Memory allocation failed\\n");\n        return 1;\n    }\n    for (int i = 0; i < 5; i++) {\n        arr[i] = (i + 1) * 10;\n    }\n    for (int i = 0; i < 5; i++) {\n        printf("%d\\n", arr[i]);\n    }\n    free(arr);\n    return 0;\n}',
      expectedOutput: '10\n20\n30\n40\n50',
      hints: ['malloc(5 * sizeof(int)) で5要素分確保', 'NULL チェックを忘れずに'],
      explanation: 'malloc はヒープ領域から指定バイト数を確保します。calloc は0初期化付き、realloc はサイズ変更。free 後のポインタは使用禁止（ダングリングポインタ）。Valgrind でメモリリーク検出ができます。',
    },
    {
      id: 13,
      title: 'ポインタのポインタ',
      difficulty: 'intermediate',
      description: 'ダブルポインタ (int **) はポインタのアドレスを格納します。2次元配列の動的確保やポインタを関数で変更する際に使います。',
      task: 'ダブルポインタを使って、関数内でポインタの指す先を変更してください。',
      initialCode: '#include <stdio.h>\n\nvoid change(int **pp, int *newAddr) {\n    // ポインタの指す先を変更\n}\n\nint main(void) {\n    int a = 10, b = 20;\n    int *p = &a;\n    printf("Before: %d\\n", *p);\n    change(&p, &b);\n    printf("After: %d\\n", *p);\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\nvoid change(int **pp, int *newAddr) {\n    *pp = newAddr;\n}\n\nint main(void) {\n    int a = 10, b = 20;\n    int *p = &a;\n    printf("Before: %d\\n", *p);\n    change(&p, &b);\n    printf("After: %d\\n", *p);\n    return 0;\n}',
      expectedOutput: 'Before: 10\nAfter: 20',
      hints: ['*pp = newAddr; でポインタ自体を変更', '&p でポインタのアドレスを渡す'],
      explanation: 'int **pp: pp はポインタへのポインタ。*pp でポインタにアクセス、**pp で値にアクセス。main()のargv(char **argv)もダブルポインタです。2次元動的配列: int **matrix = malloc(rows * sizeof(int*)); のように使います。',
    },
    {
      id: 14,
      title: '関数ポインタ',
      difficulty: 'intermediate',
      description: '関数もアドレスを持ち、ポインタに格納できます。コールバック関数やqsortの比較関数に使われます。',
      task: 'add と multiply の関数ポインタを使い、動的に演算を切り替えてください。',
      initialCode: '#include <stdio.h>\n\nint add(int a, int b) { return a + b; }\nint multiply(int a, int b) { return a * b; }\n\nint main(void) {\n    // 関数ポインタで演算を切り替え\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\nint add(int a, int b) { return a + b; }\nint multiply(int a, int b) { return a * b; }\n\nint main(void) {\n    int (*op)(int, int);\n    op = add;\n    printf("Add: %d\\n", op(3, 4));\n    op = multiply;\n    printf("Multiply: %d\\n", op(3, 4));\n    return 0;\n}',
      expectedOutput: 'Add: 7\nMultiply: 12',
      hints: ['int (*op)(int, int); で関数ポインタ宣言', 'op = add; で関数を代入'],
      explanation: '関数ポインタの型: 戻り値型 (*名前)(引数型リスト)。typedef int (*Operation)(int, int); で型名を定義すると読みやすくなります。qsort() のcompar引数は関数ポインタの実用例です。',
    },
    {
      id: 15,
      title: 'ファイル入出力',
      difficulty: 'intermediate',
      description: 'fopen, fprintf, fscanf, fclose でファイルの読み書きを行います。エラーハンドリングも重要です。',
      task: 'ファイルに書き込み、読み込んで表示するプログラムを書いてください。',
      initialCode: '#include <stdio.h>\n\nint main(void) {\n    // ファイル書き込み → 読み込み\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\nint main(void) {\n    FILE *fp = fopen("test.txt", "w");\n    if (fp == NULL) {\n        printf("Error opening file\\n");\n        return 1;\n    }\n    fprintf(fp, "Hello, File I/O!\\n");\n    fclose(fp);\n\n    char buf[100];\n    fp = fopen("test.txt", "r");\n    if (fp != NULL) {\n        fgets(buf, sizeof(buf), fp);\n        printf("%s", buf);\n        fclose(fp);\n    }\n    return 0;\n}',
      expectedOutput: 'Hello, File I/O!',
      hints: ['fopen("file", "w") で書き込みモード', 'fclose() でファイルを閉じる'],
      explanation: 'モード: "r"(読込), "w"(書込/新規), "a"(追記), "rb"(バイナリ読込)。fgets は行読み込み、fread/fwrite はバイナリ操作。ファイルを閉じ忘れるとデータ損失やリソースリークの原因になります。',
    },
    {
      id: 16,
      title: 'プリプロセッサ',
      difficulty: 'intermediate',
      description: '#define, #ifdef, #ifndef, #include などのプリプロセッサ指令はコンパイル前にソースコードを変換します。',
      task: '#define でマクロ定数と関数マクロを作ってください。',
      initialCode: '#include <stdio.h>\n\n// マクロ定数とマクロ関数を定義\n\nint main(void) {\n    printf("PI = %.2f\\n", PI);\n    printf("MAX(3, 5) = %d\\n", MAX(3, 5));\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\n#define PI 3.14159\n#define MAX(a, b) ((a) > (b) ? (a) : (b))\n\nint main(void) {\n    printf("PI = %.2f\\n", PI);\n    printf("MAX(3, 5) = %d\\n", MAX(3, 5));\n    return 0;\n}',
      expectedOutput: 'PI = 3.14\nMAX(3, 5) = 5',
      hints: ['#define PI 3.14159', '#define MAX(a, b) (...) で関数マクロ'],
      explanation: 'マクロの引数は必ず括弧で囲む: ((a) > (b)) としないと演算子の優先順位で問題が起きます。#ifndef HEADER_H ... #endif はインクルードガードです。C11の_Generic でジェネリックマクロも作れます。',
    },
    {
      id: 17,
      title: '列挙型 (enum) と共用体 (union)',
      difficulty: 'intermediate',
      description: 'enum は名前付き定数の集合、union は同じメモリ領域を異なる型で共有するデータ型です。',
      task: 'enum Color と union Value を定義して使ってください。',
      initialCode: '#include <stdio.h>\n\n// enum と union を定義\n\nint main(void) {\n    // 使用例\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\ntypedef enum { RED, GREEN, BLUE } Color;\n\ntypedef union {\n    int i;\n    float f;\n    char c;\n} Value;\n\nint main(void) {\n    Color color = GREEN;\n    printf("Color: %d\\n", color);\n\n    Value v;\n    v.i = 42;\n    printf("Int: %d\\n", v.i);\n    v.f = 3.14f;\n    printf("Float: %.2f\\n", v.f);\n    return 0;\n}',
      expectedOutput: 'Color: 1\nInt: 42\nFloat: 3.14',
      hints: ['enum { RED, GREEN, BLUE } は 0, 1, 2 に対応', 'union は最大メンバのサイズ分のメモリを使う'],
      explanation: 'enum は定数に名前を付けてコードの可読性を向上させます。union は tagged union パターンで型安全なバリアント型を実現できます。sizeof(union) は最大メンバのサイズです。',
    },
    {
      id: 18,
      title: '連結リスト',
      difficulty: 'intermediate',
      description: '連結リスト(Linked List)は動的なデータ構造で、各ノードが次のノードへのポインタを持ちます。',
      task: '3つのノードを持つ連結リストを作成し、全ノードの値を表示してください。',
      initialCode: '#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct Node {\n    int data;\n    struct Node *next;\n} Node;\n\n// リスト操作関数とmainを実装',
      solutionCode: '#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct Node {\n    int data;\n    struct Node *next;\n} Node;\n\nNode* createNode(int data) {\n    Node *n = (Node *)malloc(sizeof(Node));\n    n->data = data;\n    n->next = NULL;\n    return n;\n}\n\nvoid printList(Node *head) {\n    Node *cur = head;\n    while (cur != NULL) {\n        printf("%d\\n", cur->data);\n        cur = cur->next;\n    }\n}\n\nvoid freeList(Node *head) {\n    Node *tmp;\n    while (head != NULL) {\n        tmp = head;\n        head = head->next;\n        free(tmp);\n    }\n}\n\nint main(void) {\n    Node *head = createNode(10);\n    head->next = createNode(20);\n    head->next->next = createNode(30);\n    printList(head);\n    freeList(head);\n    return 0;\n}',
      expectedOutput: '10\n20\n30',
      hints: ['struct Node に next ポインタを持たせる', 'malloc で各ノードを動的確保'],
      explanation: '連結リストの利点: 動的サイズ、先頭/中間への挿入がO(1)。欠点: ランダムアクセスがO(n)、キャッシュ効率が悪い。双方向リスト(prev/nextの2つのポインタ)やcircularリストもあります。',
    },
    {
      id: 19,
      title: 'ビット演算',
      difficulty: 'intermediate',
      description: 'ビット演算子: &(AND), |(OR), ^(XOR), ~(NOT), <<(左シフト), >>(右シフト)。低レベルプログラミングやフラグ管理に使います。',
      task: 'ビット演算でフラグの設定・確認・クリアを行ってください。',
      initialCode: '#include <stdio.h>\n\n#define FLAG_READ  0x01\n#define FLAG_WRITE 0x02\n#define FLAG_EXEC  0x04\n\nint main(void) {\n    unsigned int perms = 0;\n    // フラグの設定・確認・クリア\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\n#define FLAG_READ  0x01\n#define FLAG_WRITE 0x02\n#define FLAG_EXEC  0x04\n\nint main(void) {\n    unsigned int perms = 0;\n    perms |= FLAG_READ;\n    perms |= FLAG_WRITE;\n    printf("Read: %d\\n", (perms & FLAG_READ) ? 1 : 0);\n    printf("Write: %d\\n", (perms & FLAG_WRITE) ? 1 : 0);\n    printf("Exec: %d\\n", (perms & FLAG_EXEC) ? 1 : 0);\n    perms &= ~FLAG_WRITE;\n    printf("Write after clear: %d\\n", (perms & FLAG_WRITE) ? 1 : 0);\n    return 0;\n}',
      expectedOutput: 'Read: 1\nWrite: 1\nExec: 0\nWrite after clear: 0',
      hints: ['|= でフラグ設定、& で確認、&= ~ でクリア', '0x01 = 0b00000001'],
      explanation: 'フラグ操作: 設定(|=)、確認(&)、クリア(&= ~)、トグル(^=)。Unix のファイルパーミッション(rwx = 0b111 = 7)もビットフラグです。組み込みシステムのレジスタ操作にも必須です。',
    },
    {
      id: 20,
      title: 'マルチファイル構成とヘッダファイル',
      difficulty: 'intermediate',
      description: '大規模プログラムは複数ファイルに分割します。ヘッダファイル(.h)で宣言、ソースファイル(.c)で実装を分離します。',
      task: 'ヘッダファイルにプロトタイプ宣言を書き、分離した構成を示してください。',
      initialCode: '/* -- math_utils.h の内容 -- */\n/* ヘッダガード + プロトタイプ宣言 */\n\n/* -- math_utils.c の内容 -- */\n/* 実装 */\n\n/* -- main.c の内容 -- */\n#include <stdio.h>\n// #include "math_utils.h"\n\nint add(int a, int b);\nint multiply(int a, int b);\n\nint main(void) {\n    printf("Add: %d\\n", add(3, 4));\n    printf("Multiply: %d\\n", multiply(3, 4));\n    return 0;\n}',
      solutionCode: '/* -- math_utils.h -- */\n#ifndef MATH_UTILS_H\n#define MATH_UTILS_H\nint add(int a, int b);\nint multiply(int a, int b);\n#endif\n\n/* -- math_utils.c -- */\n/* #include "math_utils.h" */\n\nint add(int a, int b) { return a + b; }\nint multiply(int a, int b) { return a * b; }\n\n/* -- main.c -- */\n#include <stdio.h>\n/* #include "math_utils.h" */\n\nint add(int a, int b) { return a + b; }\nint multiply(int a, int b) { return a * b; }\n\nint main(void) {\n    printf("Add: %d\\n", add(3, 4));\n    printf("Multiply: %d\\n", multiply(3, 4));\n    return 0;\n}',
      expectedOutput: 'Add: 7\nMultiply: 12',
      hints: ['#ifndef HEADER_H ... #endif でインクルードガード', '宣言(.h)と実装(.c)を分離'],
      explanation: 'コンパイル手順: gcc -c main.c math_utils.c でオブジェクトファイル生成 → gcc main.o math_utils.o -o program でリンク。#pragma once もインクルードガードの代替です。extern でグローバル変数を他ファイルから参照します。',
    },
    // ── Advanced (21-30) ──
    {
      id: 21,
      title: 'メモリレイアウトとスタック/ヒープ',
      difficulty: 'advanced',
      description: 'プログラムのメモリは Text, Data, BSS, Heap, Stack の5セグメントに分かれます。',
      task: 'スタック変数、ヒープ変数、グローバル変数、静的変数のアドレスを表示して違いを確認してください。',
      initialCode: '#include <stdio.h>\n#include <stdlib.h>\n\nint global_var = 100;\n\nint main(void) {\n    int stack_var = 200;\n    static int static_var = 300;\n    int *heap_var = (int *)malloc(sizeof(int));\n    *heap_var = 400;\n    // 各変数のアドレスを表示\n    free(heap_var);\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n#include <stdlib.h>\n\nint global_var = 100;\n\nint main(void) {\n    int stack_var = 200;\n    static int static_var = 300;\n    int *heap_var = (int *)malloc(sizeof(int));\n    *heap_var = 400;\n    printf("Global: %p (val=%d)\\n", (void*)&global_var, global_var);\n    printf("Static: %p (val=%d)\\n", (void*)&static_var, static_var);\n    printf("Stack:  %p (val=%d)\\n", (void*)&stack_var, stack_var);\n    printf("Heap:   %p (val=%d)\\n", (void*)heap_var, *heap_var);\n    free(heap_var);\n    return 0;\n}',
      expectedOutput: 'Global: 0x... (val=100)\nStatic: 0x... (val=300)\nStack:  0x... (val=200)\nHeap:   0x... (val=400)',
      hints: ['%p でポインタ（アドレス）を表示', 'スタックは高位→低位、ヒープは低位→高位に伸びる'],
      explanation: 'Text: コード、Data: 初期化済みグローバル/static、BSS: 未初期化グローバル/static、Heap: malloc等の動的確保（低位→高位）、Stack: 関数のローカル変数（高位→低位）。スタックオーバーフローとヒープの断片化は重要な問題です。',
    },
    {
      id: 22,
      title: 'void ポインタとジェネリックプログラミング',
      difficulty: 'advanced',
      description: 'void* は型を持たない汎用ポインタで、任意の型のポインタを格納できます。qsort のような汎用関数の実装に不可欠です。',
      task: 'void* と比較関数を使った汎用 swap 関数を実装してください。',
      initialCode: '#include <stdio.h>\n#include <string.h>\n\n// 汎用 swap 関数\nvoid generic_swap(void *a, void *b, size_t size) {\n    // memcpy を使って入れ替え\n}\n\nint main(void) {\n    int x = 10, y = 20;\n    printf("Before: x=%d, y=%d\\n", x, y);\n    generic_swap(&x, &y, sizeof(int));\n    printf("After: x=%d, y=%d\\n", x, y);\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n#include <string.h>\n#include <stdlib.h>\n\nvoid generic_swap(void *a, void *b, size_t size) {\n    void *tmp = malloc(size);\n    memcpy(tmp, a, size);\n    memcpy(a, b, size);\n    memcpy(b, tmp, size);\n    free(tmp);\n}\n\nint main(void) {\n    int x = 10, y = 20;\n    printf("Before: x=%d, y=%d\\n", x, y);\n    generic_swap(&x, &y, sizeof(int));\n    printf("After: x=%d, y=%d\\n", x, y);\n    return 0;\n}',
      expectedOutput: 'Before: x=10, y=20\nAfter: x=20, y=10',
      hints: ['memcpy(dst, src, size) でバイト単位コピー', 'void* は算術演算不可、キャストが必要'],
      explanation: 'void* はC言語のジェネリクスです。qsort, bsearch, memcpy すべて void* を使います。void* の算術演算（p+1 等）はGCC拡張では動きますが標準Cでは非定義です。キャストして使います。',
    },
    {
      id: 23,
      title: 'qsort を使ったソート',
      difficulty: 'advanced',
      description: 'stdlib.h の qsort は汎用ソート関数で、比較関数を関数ポインタで渡します。',
      task: 'qsort で int 配列を昇順にソートしてください。',
      initialCode: '#include <stdio.h>\n#include <stdlib.h>\n\n// 比較関数を定義\n\nint main(void) {\n    int arr[] = {42, 15, 8, 23, 4, 16};\n    int n = sizeof(arr) / sizeof(arr[0]);\n    // qsort でソート\n    for (int i = 0; i < n; i++) printf("%d ", arr[i]);\n    printf("\\n");\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n#include <stdlib.h>\n\nint compare(const void *a, const void *b) {\n    return (*(int *)a - *(int *)b);\n}\n\nint main(void) {\n    int arr[] = {42, 15, 8, 23, 4, 16};\n    int n = sizeof(arr) / sizeof(arr[0]);\n    qsort(arr, n, sizeof(int), compare);\n    for (int i = 0; i < n; i++) printf("%d ", arr[i]);\n    printf("\\n");\n    return 0;\n}',
      expectedOutput: '4 8 15 16 23 42',
      hints: ['int compare(const void *a, const void *b)', 'qsort(配列, 要素数, 要素サイズ, 比較関数)'],
      explanation: '比較関数の戻り値: 負(a<b), 0(a==b), 正(a>b)。int の比較で a-b は INT_MIN-INT_MAX でオーバーフローの危険があります。安全な実装: return (a > b) - (a < b);。構造体のソートにも同じパターンが使えます。',
    },
    {
      id: 24,
      title: 'マクロの高度なテクニック',
      difficulty: 'advanced',
      description: 'マクロの文字列化(#)、トークン連結(##)、可変長引数(__VA_ARGS__)など高度な技法を学びます。',
      task: 'デバッグ用のLOGマクロを作ってください（ファイル名・行番号付き）。',
      initialCode: '#include <stdio.h>\n\n// LOG マクロを定義\n\nint main(void) {\n    int x = 42;\n    LOG("x = %d", x);\n    LOG("hello %s", "world");\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\n#define LOG(fmt, ...) \\\n    printf("[%s:%d] " fmt "\\n", __FILE__, __LINE__, __VA_ARGS__)\n\nint main(void) {\n    int x = 42;\n    LOG("x = %d", x);\n    LOG("hello %s", "world");\n    return 0;\n}',
      expectedOutput: '[main.c:7] x = 42\n[main.c:8] hello world',
      hints: ['__FILE__ と __LINE__ は組み込みマクロ', '__VA_ARGS__ で可変長引数'],
      explanation: '#x で x を文字列化、a##b でトークン連結。__FILE__, __LINE__, __func__ は組み込みマクロ。do { ... } while(0) でマクロを安全に複数文にまとめる技法もあります。',
    },
    {
      id: 25,
      title: '再帰とスタックオーバーフロー',
      difficulty: 'advanced',
      description: '再帰関数は自分自身を呼び出します。末尾再帰最適化とスタック消費の理解が重要です。',
      task: 'フィボナッチ数列をメモ化再帰で効率的に計算してください。',
      initialCode: '#include <stdio.h>\n\nlong long memo[100] = {0};\n\nlong long fib(int n) {\n    // メモ化再帰\n}\n\nint main(void) {\n    for (int i = 0; i <= 10; i++) {\n        printf("fib(%d) = %lld\\n", i, fib(i));\n    }\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\nlong long memo[100] = {0};\n\nlong long fib(int n) {\n    if (n <= 1) return n;\n    if (memo[n] != 0) return memo[n];\n    memo[n] = fib(n - 1) + fib(n - 2);\n    return memo[n];\n}\n\nint main(void) {\n    for (int i = 0; i <= 10; i++) {\n        printf("fib(%d) = %lld\\n", i, fib(i));\n    }\n    return 0;\n}',
      expectedOutput: 'fib(0) = 0\nfib(1) = 1\nfib(2) = 1\nfib(3) = 2\nfib(4) = 3\nfib(5) = 5\nfib(6) = 8\nfib(7) = 13\nfib(8) = 21\nfib(9) = 34\nfib(10) = 55',
      hints: ['メモ配列で計算済みの値を保存', 'if (memo[n] != 0) return memo[n];'],
      explanation: '素朴な再帰 fib(n) は O(2^n)、メモ化で O(n) に改善。各再帰呼び出しはスタックフレームを消費し、深い再帰はスタックオーバーフローを引き起こします。末尾再帰は最適化でループに変換される場合があります（コンパイラ依存）。',
    },
    {
      id: 26,
      title: 'バイナリツリー',
      difficulty: 'advanced',
      description: '二分探索木(BST)は左の子 < 親 < 右の子の性質を持つデータ構造です。探索・挿入が O(log n) です。',
      task: 'BST の挿入と中順走査（昇順出力）を実装してください。',
      initialCode: '#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct TreeNode {\n    int data;\n    struct TreeNode *left, *right;\n} TreeNode;\n\n// insert と inorder を実装\n\nint main(void) {\n    TreeNode *root = NULL;\n    int values[] = {50, 30, 70, 20, 40, 60, 80};\n    for (int i = 0; i < 7; i++) {\n        root = insert(root, values[i]);\n    }\n    inorder(root);\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct TreeNode {\n    int data;\n    struct TreeNode *left, *right;\n} TreeNode;\n\nTreeNode* createNode(int data) {\n    TreeNode *n = (TreeNode *)malloc(sizeof(TreeNode));\n    n->data = data;\n    n->left = n->right = NULL;\n    return n;\n}\n\nTreeNode* insert(TreeNode *root, int data) {\n    if (root == NULL) return createNode(data);\n    if (data < root->data) root->left = insert(root->left, data);\n    else if (data > root->data) root->right = insert(root->right, data);\n    return root;\n}\n\nvoid inorder(TreeNode *root) {\n    if (root == NULL) return;\n    inorder(root->left);\n    printf("%d ", root->data);\n    inorder(root->right);\n}\n\nint main(void) {\n    TreeNode *root = NULL;\n    int values[] = {50, 30, 70, 20, 40, 60, 80};\n    for (int i = 0; i < 7; i++) {\n        root = insert(root, values[i]);\n    }\n    inorder(root);\n    printf(\"\\n\");\n    return 0;\n}',
      expectedOutput: '20 30 40 50 60 70 80',
      hints: ['再帰で左→自分→右の順で走査', 'data < root->data なら左部分木に挿入'],
      explanation: '走査: 前順(根→左→右)、中順(左→根→右=昇順)、後順(左→右→根)。偏った木(最悪O(n))を防ぐにはAVL木や赤黒木で平衡を保ちます。',
    },
    {
      id: 27,
      title: 'ハッシュテーブル',
      difficulty: 'advanced',
      description: 'ハッシュテーブルはキーのハッシュ値でインデックスを決定し、O(1) の平均探索を実現します。衝突解決が設計の鍵です。',
      task: 'チェイン法による簡単なハッシュテーブルを実装してください。',
      initialCode: '#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\n#define TABLE_SIZE 10\n\ntypedef struct Entry {\n    char key[64];\n    int value;\n    struct Entry *next;\n} Entry;\n\nEntry *table[TABLE_SIZE] = {NULL};\n\n// hash, put, get を実装\n\nint main(void) {\n    put("apple", 100);\n    put("banana", 200);\n    printf("apple: %d\\n", get("apple"));\n    printf("banana: %d\\n", get("banana"));\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\n#define TABLE_SIZE 10\n\ntypedef struct Entry {\n    char key[64];\n    int value;\n    struct Entry *next;\n} Entry;\n\nEntry *table[TABLE_SIZE] = {NULL};\n\nunsigned int hash(const char *key) {\n    unsigned int h = 0;\n    while (*key) h = h * 31 + (unsigned char)*key++;\n    return h % TABLE_SIZE;\n}\n\nvoid put(const char *key, int value) {\n    unsigned int idx = hash(key);\n    Entry *e = (Entry *)malloc(sizeof(Entry));\n    strcpy(e->key, key);\n    e->value = value;\n    e->next = table[idx];\n    table[idx] = e;\n}\n\nint get(const char *key) {\n    unsigned int idx = hash(key);\n    Entry *e = table[idx];\n    while (e != NULL) {\n        if (strcmp(e->key, key) == 0) return e->value;\n        e = e->next;\n    }\n    return -1;\n}\n\nint main(void) {\n    put("apple", 100);\n    put("banana", 200);\n    printf("apple: %d\\n", get("apple"));\n    printf("banana: %d\\n", get("banana"));\n    return 0;\n}',
      expectedOutput: 'apple: 100\nbanana: 200',
      hints: ['h = h * 31 + c はdjb2系ハッシュ', '衝突時はリンクリストで連結（チェイン法）'],
      explanation: '衝突解決: チェイン法（リンクリスト）、オープンアドレス法（線形探索、二次探索）。負荷率(要素数/テーブルサイズ)が高くなったらリハッシュします。良いハッシュ関数は均一分布が重要です。',
    },
    {
      id: 28,
      title: 'シグナル処理',
      difficulty: 'advanced',
      description: 'signal() でプロセスへのシグナル(SIGINT, SIGTERM等)をキャッチし、カスタムハンドラを設定できます。',
      task: 'SIGINT(Ctrl+C)をキャッチしてカスタムメッセージを表示するハンドラを設定してください。',
      initialCode: '#include <stdio.h>\n#include <signal.h>\n#include <stdlib.h>\n\n// シグナルハンドラ\n\nint main(void) {\n    // SIGINT ハンドラを設定\n    printf("Running... (pid=%d)\\n", 12345);\n    printf("Signal handler set for SIGINT\\n");\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n#include <signal.h>\n#include <stdlib.h>\n\nvolatile sig_atomic_t running = 1;\n\nvoid handle_sigint(int sig) {\n    printf("\\nCaught SIGINT (signal %d)\\n", sig);\n    running = 0;\n}\n\nint main(void) {\n    signal(SIGINT, handle_sigint);\n    printf("Running... Press Ctrl+C to stop\\n");\n    printf("Signal handler set for SIGINT\\n");\n    return 0;\n}',
      expectedOutput: 'Running... Press Ctrl+C to stop\nSignal handler set for SIGINT',
      hints: ['signal(SIGINT, handler_function);', 'volatile sig_atomic_t で安全なフラグ'],
      explanation: 'signal() はPOSIX標準ですが、sigaction() がより安全で推奨されます。ハンドラ内ではasync-signal-safe な関数のみ使用可能（printf は本来NG）。volatile sig_atomic_t はシグナルハンドラから安全にアクセスできる型です。',
    },
    {
      id: 29,
      title: 'メモリ安全性とデバッグ',
      difficulty: 'advanced',
      description: 'バッファオーバーフロー、ダングリングポインタ、メモリリープ、未初期化変数 — C言語における主要なメモリバグとその検出方法を学びます。',
      task: 'メモリバグのパターンと安全な代替コードを示してください。',
      initialCode: '#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\n// 安全なメモリ操作のデモ\n\nint main(void) {\n    // 安全な文字列コピー\n    // 安全なメモリ解放\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\nchar* safe_strdup(const char *src) {\n    if (src == NULL) return NULL;\n    size_t len = strlen(src) + 1;\n    char *dst = (char *)malloc(len);\n    if (dst == NULL) return NULL;\n    memcpy(dst, src, len);\n    return dst;\n}\n\nvoid safe_free(void **ptr) {\n    if (ptr && *ptr) {\n        free(*ptr);\n        *ptr = NULL;\n    }\n}\n\nint main(void) {\n    char *str = safe_strdup("Hello, Safe C!");\n    if (str) {\n        printf("%s\\n", str);\n        safe_free((void **)&str);\n        printf("After free: %s\\n", str == NULL ? "NULL" : "NOT NULL");\n    }\n    return 0;\n}',
      expectedOutput: 'Hello, Safe C!\nAfter free: NULL',
      hints: ['free後にNULLを代入してダングリング防止', 'NULLチェックを常に行う'],
      explanation: '主要バグ: 1.バッファオーバーフロー(strncpyで防止) 2.ダングリングポインタ(free後NULL代入) 3.メモリリーク(確保と解放を対にする) 4.未初期化読み取り(常に初期化)。ツール: Valgrind, AddressSanitizer(-fsanitize=address), StaticAnalyzer。',
    },
    {
      id: 30,
      title: 'Cプリプロセッサで型安全なジェネリクス',
      difficulty: 'advanced',
      description: 'C11 の _Generic キーワードとマクロを組み合わせて、型安全なジェネリックインターフェースを作ります。',
      task: '_Generic を使って、int/double/char* に対応する汎用 print マクロを作ってください。',
      initialCode: '#include <stdio.h>\n\n// _Generic を使った print マクロ\n\nint main(void) {\n    int i = 42;\n    double d = 3.14;\n    char *s = "hello";\n    print(i);\n    print(d);\n    print(s);\n    return 0;\n}',
      solutionCode: '#include <stdio.h>\n\n#define print(x) _Generic((x), \\\n    int: print_int, \\\n    double: print_double, \\\n    char*: print_str \\\n)(x)\n\nvoid print_int(int x) { printf("int: %d\\n", x); }\nvoid print_double(double x) { printf("double: %.2f\\n", x); }\nvoid print_str(char *x) { printf("string: %s\\n", x); }\n\nint main(void) {\n    int i = 42;\n    double d = 3.14;\n    char *s = "hello";\n    print(i);\n    print(d);\n    print(s);\n    return 0;\n}',
      expectedOutput: 'int: 42\ndouble: 3.14\nstring: hello',
      hints: ['_Generic((x), int: fn_int, double: fn_double, ...)(x)', 'コンパイル時に型に応じた関数が選択される'],
      explanation: '_Generic はC11で追加されたコンパイル時の型スイッチです。C++ のテンプレートや関数オーバーロードに相当する機能をCで実現できます。tgmath.h の型ジェネリック数学関数も内部的にこれを使っています。',
    },
  ],
};

export default course;
