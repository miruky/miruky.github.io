import type { LangCourse } from './types';

const course: LangCourse = {
  id: 'javascript',
  name: 'JavaScript',
  nameJa: 'JavaScript',
  simpleIconSlug: 'javascript',
  color: '#F7DF1E',
  description: 'Webの共通言語。フロントエンド・バックエンド両方で活躍する万能言語です。',
  lessons: [
    // ── Beginner (1-10) ──
    {
      id: 1,
      title: 'Hello World — console.log',
      difficulty: 'beginner',
      description: 'JavaScriptでは `console.log()` を使って値を出力します。文字列はシングルクォートまたはダブルクォートで囲みます。',
      task: 'console.log を使って "Hello, JavaScript!" と出力してください。',
      initialCode: '// ここにコードを書いてください\n',
      solutionCode: 'console.log("Hello, JavaScript!");',
      expectedOutput: 'Hello, JavaScript!',
      hints: ['console.log() の中に文字列を入れます', '文字列はダブルクォートで囲みます'],
      explanation: 'console.log() はブラウザの開発者ツールやNode.jsのコンソールに値を出力する最も基本的な関数です。デバッグにも頻繁に使います。',
    },
    {
      id: 2,
      title: '変数宣言 — let と const',
      difficulty: 'beginner',
      description: 'JavaScriptでは `let`（再代入可能）と `const`（再代入不可）で変数を宣言します。`var` は古い書き方なので原則使いません。',
      task: 'const で name に "miruky" を、let で age に 25 を代入し、両方を出力してください。',
      initialCode: '// const で name を宣言\n\n// let で age を宣言\n\n// 出力\n',
      solutionCode: 'const name = "miruky";\nlet age = 25;\nconsole.log(name);\nconsole.log(age);',
      expectedOutput: 'miruky\n25',
      hints: ['const name = "miruky"; のように書きます', 'let age = 25; で数値を代入します'],
      explanation: 'const は定数（再代入不可）、let は変数（再代入可能）です。原則 const を使い、再代入が必要な場合のみ let を使うのがベストプラクティスです。var はスコープの問題があるため使いません。',
    },
    {
      id: 3,
      title: 'データ型 — typeof で確認',
      difficulty: 'beginner',
      description: 'JavaScriptの基本型: string, number, boolean, undefined, null, symbol, bigint。typeof 演算子で型を確認できます。',
      task: '文字列 "hello"、数値 42、真偽値 true の型をそれぞれ typeof で出力してください。',
      initialCode: 'console.log(typeof "hello");\n// 数値 42 の型を出力\n// 真偽値 true の型を出力\n',
      solutionCode: 'console.log(typeof "hello");\nconsole.log(typeof 42);\nconsole.log(typeof true);',
      expectedOutput: 'string\nnumber\nboolean',
      hints: ['typeof 42 のように書きます', 'typeof の結果は文字列として返されます'],
      explanation: 'JavaScriptは動的型付け言語で、変数の型は実行時に決まります。typeof は値の型を文字列で返します。注意: typeof null は "object" を返すバグが仕様として残っています。',
    },
    {
      id: 4,
      title: 'テンプレートリテラル',
      difficulty: 'beginner',
      description: 'バッククォート(`)で囲み、${} で変数を埋め込めるテンプレートリテラルは、文字列結合より読みやすいコードを書けます。',
      task: 'const lang = "JavaScript" を定義し、テンプレートリテラルで "I love JavaScript!" と出力してください。',
      initialCode: 'const lang = "JavaScript";\n// テンプレートリテラルで出力\n',
      solutionCode: 'const lang = "JavaScript";\nconsole.log(`I love ${lang}!`);',
      expectedOutput: 'I love JavaScript!',
      hints: ['バッククォート ` を使います', '${変数名} で埋め込みます'],
      explanation: 'テンプレートリテラルはES6で導入されました。複数行の文字列もそのまま書け、式の埋め込みも可能です。"Hello, " + name + "!" より `Hello, ${name}!` の方が読みやすくなります（リーダブルコードの原則）。',
    },
    {
      id: 5,
      title: '配列の基本',
      difficulty: 'beginner',
      description: '配列は複数の値を順序付きで格納するデータ構造です。[] で作成し、インデックス（0始まり）でアクセスします。',
      task: '配列 fruits に "apple", "banana", "cherry" を入れ、長さと2番目の要素を出力してください。',
      initialCode: 'const fruits = [];\n// 長さを出力\n// 2番目の要素（インデックス1）を出力\n',
      solutionCode: 'const fruits = ["apple", "banana", "cherry"];\nconsole.log(fruits.length);\nconsole.log(fruits[1]);',
      expectedOutput: '3\nbanana',
      hints: ['配列は ["apple", "banana", "cherry"] のように書きます', '.length で長さ、[1] で2番目の要素を取得'],
      explanation: '配列のインデックスは0から始まります。.length プロパティで要素数を取得できます。配列は Object の一種で、要素の型を混在させることもできます（が、推奨されません）。',
    },
    {
      id: 6,
      title: 'オブジェクトの基本',
      difficulty: 'beginner',
      description: 'オブジェクトはキーと値のペアを格納します。{} で作成し、ドット記法やブラケット記法でアクセスします。',
      task: 'name: "miruky", role: "engineer" を持つオブジェクト user を作り、両方のプロパティを出力してください。',
      initialCode: '// オブジェクト user を作成\n\n// name と role を出力\n',
      solutionCode: 'const user = { name: "miruky", role: "engineer" };\nconsole.log(user.name);\nconsole.log(user.role);',
      expectedOutput: 'miruky\nengineer',
      hints: ['const user = { name: "miruky", role: "engineer" };', 'user.name でアクセスします'],
      explanation: 'オブジェクトはJavaScriptの最も重要なデータ構造です。ドット記法（obj.key）が基本ですが、動的なキーにはブラケット記法（obj["key"]）を使います。',
    },
    {
      id: 7,
      title: 'if / else 条件分岐',
      difficulty: 'beginner',
      description: '条件分岐は if / else if / else で記述します。条件式が truthy なら実行されます。',
      task: 'const score = 85 を定義し、80以上なら "Great"、60以上なら "Good"、それ以外は "Keep going" を出力してください。',
      initialCode: 'const score = 85;\n// if-else で判定して出力\n',
      solutionCode: 'const score = 85;\nif (score >= 80) {\n  console.log("Great");\n} else if (score >= 60) {\n  console.log("Good");\n} else {\n  console.log("Keep going");\n}',
      expectedOutput: 'Great',
      hints: ['if (score >= 80) で条件を書きます', 'else if で追加条件を指定します'],
      explanation: 'JavaScriptでは 0, "", null, undefined, NaN が falsy、それ以外が truthy です。=== は厳密等価（型も比較）、== は抽象等価（型変換あり）です。常に === を使うのがベストプラクティスです。',
    },
    {
      id: 8,
      title: 'for ループ',
      difficulty: 'beginner',
      description: 'for ループは初期化・条件・更新の3部分で構成されます。配列の反復には for...of も使えます。',
      task: '1から5までの数字をfor文で1行ずつ出力してください。',
      initialCode: '// for ループで 1〜5 を出力\n',
      solutionCode: 'for (let i = 1; i <= 5; i++) {\n  console.log(i);\n}',
      expectedOutput: '1\n2\n3\n4\n5',
      hints: ['for (let i = 1; i <= 5; i++) と書きます', 'ループ内で console.log(i) を実行'],
      explanation: 'for文の構造: for (初期化; 条件; 更新) { 処理 }。配列の反復には for...of（値を取得）、オブジェクトには for...in（キーを取得）も使えます。forEach メソッドも一般的です。',
    },
    {
      id: 9,
      title: '関数宣言とアロー関数',
      difficulty: 'beginner',
      description: '関数は function キーワードまたはアロー関数 (=>) で定義します。モダンJSではアロー関数が主流です。',
      task: '2つの数値を受け取って合計を返すアロー関数 add を作り、add(3, 7) の結果を出力してください。',
      initialCode: '// アロー関数 add を定義\n\n// 結果を出力\n',
      solutionCode: 'const add = (a, b) => a + b;\nconsole.log(add(3, 7));',
      expectedOutput: '10',
      hints: ['const add = (a, b) => a + b; のように書きます', '1行の場合は {} と return を省略できます'],
      explanation: 'アロー関数は (引数) => 式 の形で書きます。1行なら暗黙のreturn、複数行は {} と return が必要です。アロー関数は this を束縛しない点が function と異なります。',
    },
    {
      id: 10,
      title: '配列メソッド — map / filter',
      difficulty: 'beginner',
      description: 'map は各要素を変換した新配列を、filter は条件に合う要素だけの新配列を返します。元の配列は変更しません（イミュータブル操作）。',
      task: 'const nums = [1, 2, 3, 4, 5] から偶数だけを filter し、それぞれ2倍にした配列を出力してください。',
      initialCode: 'const nums = [1, 2, 3, 4, 5];\n// filter で偶数を抽出し、map で2倍にする\n',
      solutionCode: 'const nums = [1, 2, 3, 4, 5];\nconst result = nums.filter(n => n % 2 === 0).map(n => n * 2);\nconsole.log(result);',
      expectedOutput: '4,8',
      hints: ['filter(n => n % 2 === 0) で偶数を抽出', '.map(n => n * 2) でチェーンします'],
      explanation: 'map, filter, reduce はJavaScript関数型プログラミングの三本柱です。メソッドチェーンで組み合わせることで、読みやすく副作用のないコードが書けます。元の配列は変更されません。',
    },
    // ── Intermediate (11-20) ──
    {
      id: 11,
      title: '分割代入 (Destructuring)',
      difficulty: 'intermediate',
      description: '配列やオブジェクトから値を取り出して変数に代入する構文です。コードの簡潔さが大幅に向上します。',
      task: 'const user = { name: "miruky", age: 25, city: "Tokyo" } から分割代入で取り出し、"miruky is 25 in Tokyo" と出力してください。',
      initialCode: 'const user = { name: "miruky", age: 25, city: "Tokyo" };\n// 分割代入で取り出し\n// テンプレートリテラルで出力\n',
      solutionCode: 'const user = { name: "miruky", age: 25, city: "Tokyo" };\nconst { name, age, city } = user;\nconsole.log(`${name} is ${age} in ${city}`);',
      expectedOutput: 'miruky is 25 in Tokyo',
      hints: ['const { name, age, city } = user; と書きます', 'テンプレートリテラルで結合します'],
      explanation: 'オブジェクトの分割代入は { プロパティ名 } = オブジェクト の形で書きます。配列は [a, b] = array の形です。デフォルト値 { name = "unknown" } やリネーム { name: userName } も可能です。',
    },
    {
      id: 12,
      title: 'スプレッド構文と Rest パラメータ',
      difficulty: 'intermediate',
      description: '... はスプレッド（展開）とレスト（残余）の2つの使い方があります。配列・オブジェクトのコピーやマージに便利です。',
      task: '配列 a = [1, 2] と b = [3, 4] をスプレッド構文で結合し、結果を出力してください。',
      initialCode: 'const a = [1, 2];\nconst b = [3, 4];\n// スプレッド構文で結合\n',
      solutionCode: 'const a = [1, 2];\nconst b = [3, 4];\nconst merged = [...a, ...b];\nconsole.log(merged);',
      expectedOutput: '1,2,3,4',
      hints: ['[...a, ...b] で2つの配列を展開して結合', 'オブジェクトでも { ...obj1, ...obj2 } が使えます'],
      explanation: 'スプレッド構文は配列やオブジェクトを「展開」します。浅いコピーにも使えます: const copy = [...arr] や { ...obj }。Rest パラメータは function(...args) のように残りの引数を配列として受け取ります。',
    },
    {
      id: 13,
      title: 'Promise と非同期処理の基本',
      difficulty: 'intermediate',
      description: 'JavaScriptはシングルスレッドですが、Promiseで非同期処理を扱います。resolve で成功、reject で失敗を表現します。',
      task: '1秒後に "Done!" を返す Promise を作り、.then() で結果を出力してください（setTimeoutは使わず、即座にresolveしてOK）。',
      initialCode: '// Promise を作成\nconst promise = new Promise((resolve) => {\n  // resolve で値を返す\n});\n\n// .then で結果を出力\n',
      solutionCode: 'const promise = new Promise((resolve) => {\n  resolve("Done!");\n});\n\npromise.then((result) => {\n  console.log(result);\n});',
      expectedOutput: 'Done!',
      hints: ['resolve("Done!") で成功値を渡します', '.then((result) => console.log(result)) でチェーン'],
      explanation: 'Promiseは非同期処理の結果を表すオブジェクトです。pending（待機）→ fulfilled（成功）or rejected（失敗）の3状態があります。.then() で成功時、.catch() でエラー時の処理を記述します。',
    },
    {
      id: 14,
      title: 'async / await',
      difficulty: 'intermediate',
      description: 'async/await は Promise をより読みやすく書くための構文糖です。async 関数内で await を使うと、Promise の解決を待てます。',
      task: '"Hello" を返す async 関数 greet を作り、await で結果を取得して出力してください。',
      initialCode: '// async 関数を定義\n\n// 即時実行で await して出力\n',
      solutionCode: 'async function greet() {\n  return "Hello";\n}\n\nasync function main() {\n  const msg = await greet();\n  console.log(msg);\n}\nmain();',
      expectedOutput: 'Hello',
      hints: ['async function は自動的に Promise を返します', 'return "Hello" は resolve("Hello") と同等'],
      explanation: 'async関数は常にPromiseを返します。await はPromiseが解決されるまで待機します。try/catch でエラーハンドリングできます。トップレベルではawaitが使えないため、async関数で包む必要があります。',
    },
    {
      id: 15,
      title: 'クラスの基本',
      difficulty: 'intermediate',
      description: 'ES6のクラス構文でオブジェクト指向プログラミングができます。constructor, メソッド, getter/setter を定義できます。',
      task: 'name と age を持つ Person クラスを作り、greet() メソッドで "I am miruky, age 25" を出力してください。',
      initialCode: 'class Person {\n  // constructor を定義\n\n  // greet メソッドを定義\n}\n\nconst p = new Person("miruky", 25);\np.greet();\n',
      solutionCode: 'class Person {\n  constructor(name, age) {\n    this.name = name;\n    this.age = age;\n  }\n\n  greet() {\n    console.log(`I am ${this.name}, age ${this.age}`);\n  }\n}\n\nconst p = new Person("miruky", 25);\np.greet();',
      expectedOutput: 'I am miruky, age 25',
      hints: ['constructor(name, age) で初期化します', 'this.name = name; でインスタンス変数に代入'],
      explanation: 'JavaScriptのクラスは内部的にはプロトタイプベースの継承を使っています。constructor は new で呼び出される初期化メソッドです。extends で継承、super で親クラスのメソッドを呼べます。',
    },
    {
      id: 16,
      title: 'クロージャ',
      difficulty: 'intermediate',
      description: 'クロージャは関数が定義された時点のスコープを「記憶」する仕組みです。状態を保持するために使います。',
      task: '呼び出すたびにカウントが1ずつ増えるカウンター関数を作り、3回呼び出して結果を出力してください。',
      initialCode: 'function createCounter() {\n  // クロージャでカウンターを実装\n}\n\nconst counter = createCounter();\nconsole.log(counter());\nconsole.log(counter());\nconsole.log(counter());\n',
      solutionCode: 'function createCounter() {\n  let count = 0;\n  return () => {\n    count++;\n    return count;\n  };\n}\n\nconst counter = createCounter();\nconsole.log(counter());\nconsole.log(counter());\nconsole.log(counter());',
      expectedOutput: '1\n2\n3',
      hints: ['外側の関数で let count = 0 を定義', '内側の関数から count を参照・更新します'],
      explanation: 'クロージャは関数が外側スコープの変数を参照し続ける仕組みです。カプセル化（プライベート変数の実現）やファクトリパターンに使います。モジュールパターンの基盤でもあります。',
    },
    {
      id: 17,
      title: 'reduce で集計処理',
      difficulty: 'intermediate',
      description: 'reduce は配列を1つの値に畳み込むメソッドです。合計値・最大値・グルーピングなど多用途に使えます。',
      task: 'const prices = [100, 200, 300, 400, 500] の合計を reduce で計算して出力してください。',
      initialCode: 'const prices = [100, 200, 300, 400, 500];\n// reduce で合計を計算\n',
      solutionCode: 'const prices = [100, 200, 300, 400, 500];\nconst total = prices.reduce((sum, price) => sum + price, 0);\nconsole.log(total);',
      expectedOutput: '1500',
      hints: ['reduce((sum, price) => sum + price, 0) と書きます', '第2引数 0 が初期値です'],
      explanation: 'reduce(callback, initialValue) は左から順にcallbackを実行し、累積値を返します。初期値を省略すると配列の最初の要素が使われます。mapやfilterもreduceで実装可能ですが、用途に合ったメソッドを使うのがリーダブルコードの原則です。',
    },
    {
      id: 18,
      title: 'Map と Set',
      difficulty: 'intermediate',
      description: 'Map はキーに任意の型を使えるコレクション、Set は重複のないユニークな値のコレクションです。',
      task: 'const arr = [1, 2, 2, 3, 3, 3] から Set で重複を除去し、ユニークな要素数を出力してください。',
      initialCode: 'const arr = [1, 2, 2, 3, 3, 3];\n// Set で重複除去\n',
      solutionCode: 'const arr = [1, 2, 2, 3, 3, 3];\nconst unique = new Set(arr);\nconsole.log(unique.size);',
      expectedOutput: '3',
      hints: ['new Set(arr) で重複が自動除去されます', '.size でサイズを取得（.length ではない）'],
      explanation: 'Set は重複を許さないコレクションで、has(), add(), delete() メソッドを持ちます。配列への変換は [...set] でできます。Map は { } と違いキーの順序が保証され、任意の型をキーにできます。',
    },
    {
      id: 19,
      title: 'エラーハンドリング — try/catch',
      difficulty: 'intermediate',
      description: 'try/catch は実行時エラーを捕捉して、プログラムのクラッシュを防ぎます。finally は成功・失敗に関わらず実行されます。',
      task: 'JSON.parse を try/catch で囲み、不正なJSONを渡した場合に "Parse error" と出力してください。',
      initialCode: 'const badJson = "{ invalid }";\n// try/catch でパースを試みる\n',
      solutionCode: 'const badJson = "{ invalid }";\ntry {\n  JSON.parse(badJson);\n} catch (e) {\n  console.log("Parse error");\n}',
      expectedOutput: 'Parse error',
      hints: ['try { JSON.parse(badJson) } catch (e) { ... }', 'catch ブロック内でエラーメッセージを出力'],
      explanation: 'try/catch は同期コードのエラーハンドリングに使います。async/await と組み合わせて非同期エラーも捕捉できます。catch(e) の e はErrorオブジェクトで、e.message でメッセージを取得できます。',
    },
    {
      id: 20,
      title: 'モジュール — import / export',
      difficulty: 'intermediate',
      description: 'ES Modules でコードを分割・再利用します。export で公開、import で取り込みます。名前付きエクスポートとデフォルトエクスポートがあります。',
      task: '以下のコードで add と multiply をエクスポートする書き方を完成させてください（※ブラウザ実行のため、出力で確認します）。',
      initialCode: 'function add(a, b) {\n  return a + b;\n}\n\nfunction multiply(a, b) {\n  return a * b;\n}\n\n// 関数を使って出力\nconsole.log(add(2, 3));\nconsole.log(multiply(4, 5));',
      solutionCode: 'function add(a, b) {\n  return a + b;\n}\n\nfunction multiply(a, b) {\n  return a * b;\n}\n\nconsole.log(add(2, 3));\nconsole.log(multiply(4, 5));',
      expectedOutput: '5\n20',
      hints: ['export function add(a, b) の形で公開します', '今回はブラウザ実行なのでそのまま関数を呼べばOK'],
      explanation: 'ES Modules: export function add() {} で名前付きエクスポート、export default で1つだけデフォルトエクスポートできます。import { add } from "./math" でインポートします。モジュールでコードを小さく保つのがプリンシパルオブプログラミングの原則です。',
    },
    // ── Advanced (21-30) ──
    {
      id: 21,
      title: 'ジェネレータ関数',
      difficulty: 'advanced',
      description: 'function* で定義するジェネレータは yield で値を1つずつ返す遅延評価の仕組みです。イテレータプロトコルを実装します。',
      task: '1, 2, 3 を yield するジェネレータ関数を作り、3つの値を順に出力してください。',
      initialCode: 'function* numbers() {\n  // yield で値を返す\n}\n\nconst gen = numbers();\nconsole.log(gen.next().value);\nconsole.log(gen.next().value);\nconsole.log(gen.next().value);\n',
      solutionCode: 'function* numbers() {\n  yield 1;\n  yield 2;\n  yield 3;\n}\n\nconst gen = numbers();\nconsole.log(gen.next().value);\nconsole.log(gen.next().value);\nconsole.log(gen.next().value);',
      expectedOutput: '1\n2\n3',
      hints: ['yield 1; yield 2; yield 3; と書きます', '.next() で次の値を取得、.value で値を参照'],
      explanation: 'ジェネレータは遅延評価を実現し、無限シーケンスや大量データの逐次処理に有効です。for...of でもイテレートできます。Redux-Saga などのライブラリもジェネレータを活用しています。',
    },
    {
      id: 22,
      title: 'Proxy と Reflect',
      difficulty: 'advanced',
      description: 'Proxy はオブジェクトの操作（get, set, deleteなど）をインターセプトするメタプログラミング機能です。',
      task: 'プロパティ取得時にログを出力するProxyを作り、user.name にアクセスして "Getting name" の後に "miruky" を出力してください。',
      initialCode: 'const user = { name: "miruky" };\n\nconst proxy = new Proxy(user, {\n  get(target, prop) {\n    // ログを出力してから値を返す\n  }\n});\n\nconsole.log(proxy.name);\n',
      solutionCode: 'const user = { name: "miruky" };\n\nconst proxy = new Proxy(user, {\n  get(target, prop) {\n    console.log(`Getting ${String(prop)}`);\n    return Reflect.get(target, prop);\n  }\n});\n\nconsole.log(proxy.name);',
      expectedOutput: 'Getting name\nmiruky',
      hints: ['get(target, prop) トラップで操作をインターセプト', 'Reflect.get(target, prop) で元の操作を実行'],
      explanation: 'Proxyはバリデーション、ロギング、リアクティブシステム（Vue.js 3）などに使われます。Reflect は Proxy のトラップに対応するメソッドを持ち、元の操作を安全に実行できます。',
    },
    {
      id: 23,
      title: 'WeakMap と WeakRef',
      difficulty: 'advanced',
      description: 'WeakMap はキーがGC対象になれる弱参照のMapです。メモリリークを防ぎつつプライベートデータを管理できます。',
      task: 'WeakMap を使ってオブジェクトにプライベートデータ(count: 0)を紐付け、カウントを増やして出力してください。',
      initialCode: 'const privateData = new WeakMap();\n\nconst obj = {};\n// obj にプライベートデータ {count: 0} を紐付け\n// count を 1 に更新して出力\n',
      solutionCode: 'const privateData = new WeakMap();\n\nconst obj = {};\nprivateData.set(obj, { count: 0 });\nprivateData.get(obj).count = 1;\nconsole.log(privateData.get(obj).count);',
      expectedOutput: '1',
      hints: ['privateData.set(obj, { count: 0 }) で紐付け', 'privateData.get(obj).count で値を取得'],
      explanation: 'WeakMap のキーはオブジェクトのみで、キーへの参照が他になければGCされます。DOMノードへのメタデータ紐付けやクラスのプライベートフィールドの実装に使われます。WeakSet も同様の弱参照セットです。',
    },
    {
      id: 24,
      title: 'イベントループの理解',
      difficulty: 'advanced',
      description: 'JavaScriptのイベントループは、コールスタック → マイクロタスク(Promise) → マクロタスク(setTimeout) の順に実行します。',
      task: '以下のコードの実行順序を出力で示してください: 同期 → Promise → setTimeout の順で実行されます。',
      initialCode: 'console.log("1: sync");\n\nsetTimeout(() => {\n  console.log("3: timeout");\n}, 0);\n\nPromise.resolve().then(() => {\n  console.log("2: promise");\n});\n',
      solutionCode: 'console.log("1: sync");\n\nsetTimeout(() => {\n  console.log("3: timeout");\n}, 0);\n\nPromise.resolve().then(() => {\n  console.log("2: promise");\n});',
      expectedOutput: '1: sync\n2: promise\n3: timeout',
      hints: ['同期コードが最初に実行されます', 'マイクロタスク(Promise)がマクロタスク(setTimeout)より先に実行'],
      explanation: 'イベントループ: ①コールスタックの同期コード実行 → ②マイクロタスクキュー（Promise.then, queueMicrotask）を全部実行 → ③マクロタスク（setTimeout, setInterval）を1つ実行 → ②に戻る。この理解はJSの非同期処理の根幹です。',
    },
    {
      id: 25,
      title: 'カリー化と部分適用',
      difficulty: 'advanced',
      description: 'カリー化は複数引数の関数を、1引数ずつ受け取る関数のチェーンに変換する技法です。関数の再利用性が向上します。',
      task: 'カリー化された add 関数を作り、add(2)(3) が 5 を返すようにして出力してください。',
      initialCode: '// カリー化された add 関数\n\nconsole.log(add(2)(3));\n',
      solutionCode: 'const add = (a) => (b) => a + b;\n\nconsole.log(add(2)(3));',
      expectedOutput: '5',
      hints: ['const add = (a) => (b) => a + b;', '関数を返す関数です'],
      explanation: 'カリー化は関数型プログラミングの重要な概念です。部分適用（const add2 = add(2)）で特化した関数を作れます。ramda や lodash/fp でよく使われるパターンです。',
    },
    {
      id: 26,
      title: 'デザインパターン — Observer',
      difficulty: 'advanced',
      description: 'Observer パターンはあるオブジェクトの状態変化を他のオブジェクトに通知する仕組みです。イベント駆動の基盤です。',
      task: 'subscribe/notify メソッドを持つ EventEmitter クラスを作り、"hello" イベントで "World" と出力してください。',
      initialCode: 'class EventEmitter {\n  constructor() {\n    this.listeners = {};\n  }\n  // subscribe メソッド\n  // notify メソッド\n}\n\nconst emitter = new EventEmitter();\nemitter.subscribe("hello", (data) => console.log(data));\nemitter.notify("hello", "World");\n',
      solutionCode: 'class EventEmitter {\n  constructor() {\n    this.listeners = {};\n  }\n  subscribe(event, fn) {\n    if (!this.listeners[event]) this.listeners[event] = [];\n    this.listeners[event].push(fn);\n  }\n  notify(event, data) {\n    if (this.listeners[event]) {\n      this.listeners[event].forEach(fn => fn(data));\n    }\n  }\n}\n\nconst emitter = new EventEmitter();\nemitter.subscribe("hello", (data) => console.log(data));\nemitter.notify("hello", "World");',
      expectedOutput: 'World',
      hints: ['subscribe でリスナーを配列に追加', 'notify で該当イベントのリスナーを全て呼び出す'],
      explanation: 'Observer パターンは疎結合な通知システムを実現します。Node.js の EventEmitter、ブラウザの addEventListener、React の状態管理（Redux）など、JavaScriptのあらゆる場所で使われています。',
    },
    {
      id: 27,
      title: 'Symbol とイテレータプロトコル',
      difficulty: 'advanced',
      description: 'Symbol はユニークな識別子を作る型です。Symbol.iterator を実装すると for...of で反復可能なオブジェクトを作れます。',
      task: '1〜3を返すカスタムイテラブルオブジェクトを作り、for...of で値を出力してください。',
      initialCode: 'const range = {\n  [Symbol.iterator]() {\n    let current = 1;\n    return {\n      next() {\n        // 1, 2, 3 を返し、4で終了\n      }\n    };\n  }\n};\n\nfor (const n of range) {\n  console.log(n);\n}\n',
      solutionCode: 'const range = {\n  [Symbol.iterator]() {\n    let current = 1;\n    return {\n      next() {\n        if (current <= 3) {\n          return { value: current++, done: false };\n        }\n        return { value: undefined, done: true };\n      }\n    };\n  }\n};\n\nfor (const n of range) {\n  console.log(n);\n}',
      expectedOutput: '1\n2\n3',
      hints: ['{ value: current++, done: false } で値を返す', 'done: true で反復終了を示す'],
      explanation: 'イテレータプロトコル: next() が { value, done } を返すオブジェクト。Symbol.iterator を実装するとfor...of, スプレッド構文, 分割代入で使えます。Array, Map, Set, String は全てイテラブルです。',
    },
    {
      id: 28,
      title: 'Promise.allSettled と並行処理',
      difficulty: 'advanced',
      description: 'Promise.all は1つでも失敗すると reject されますが、Promise.allSettled は全ての結果を取得できます。',
      task: '成功する Promise と失敗する Promise を allSettled で処理し、各結果の status を出力してください。',
      initialCode: 'const p1 = Promise.resolve("OK");\nconst p2 = Promise.reject("Error");\n\n// Promise.allSettled で両方の結果を取得\n',
      solutionCode: 'const p1 = Promise.resolve("OK");\nconst p2 = Promise.reject("Error");\n\nPromise.allSettled([p1, p2]).then((results) => {\n  results.forEach(r => console.log(r.status));\n});',
      expectedOutput: 'fulfilled\nrejected',
      hints: ['Promise.allSettled([p1, p2]).then(results => ...)', '各 result は { status, value/reason } を持つ'],
      explanation: 'Promise.all: 全成功 or 1つ失敗で即reject。Promise.allSettled: 全完了を待つ。Promise.race: 最初に決着したもの。Promise.any: 最初に成功したもの。用途に応じて使い分けます。',
    },
    {
      id: 29,
      title: 'メモ化関数の実装',
      difficulty: 'advanced',
      description: 'メモ化は同じ引数での関数呼び出し結果をキャッシュし、計算を省略するパフォーマンス最適化手法です。',
      task: '汎用メモ化関数 memoize を実装し、重い計算のキャッシュを確認してください。',
      initialCode: 'function memoize(fn) {\n  // キャッシュを Map で管理し、メモ化を実装\n}\n\nconst double = memoize((n) => {\n  console.log("computing...");\n  return n * 2;\n});\n\nconsole.log(double(5));\nconsole.log(double(5));\n',
      solutionCode: 'function memoize(fn) {\n  const cache = new Map();\n  return function(...args) {\n    const key = JSON.stringify(args);\n    if (cache.has(key)) return cache.get(key);\n    const result = fn(...args);\n    cache.set(key, result);\n    return result;\n  };\n}\n\nconst double = memoize((n) => {\n  console.log("computing...");\n  return n * 2;\n});\n\nconsole.log(double(5));\nconsole.log(double(5));',
      expectedOutput: 'computing...\n10\n10',
      hints: ['Map をキャッシュとして使います', 'JSON.stringify(args) をキーにします'],
      explanation: 'メモ化はフィボナッチ数列のような再帰計算やAPIレスポンスのキャッシュに有効です。React の useMemo, useCallback もメモ化の一種です。キャッシュの肥大化に注意し、LRUキャッシュの導入も検討しましょう。',
    },
    {
      id: 30,
      title: '関数型パイプラインの構築',
      difficulty: 'advanced',
      description: '小さな純粋関数を組み合わせてデータを変換するパイプラインは、関数型プログラミングの真髄です。',
      task: 'pipe 関数を実装し、数値を2倍 → 3を足す → 文字列化するパイプラインを作って 5 に適用してください。',
      initialCode: 'const pipe = (...fns) => {\n  // 関数を左から順に適用する\n};\n\nconst double = (n) => n * 2;\nconst addThree = (n) => n + 3;\nconst toString = (n) => `Result: ${n}`;\n\nconst transform = pipe(double, addThree, toString);\nconsole.log(transform(5));\n',
      solutionCode: 'const pipe = (...fns) => (x) => fns.reduce((v, fn) => fn(v), x);\n\nconst double = (n) => n * 2;\nconst addThree = (n) => n + 3;\nconst toString = (n) => `Result: ${n}`;\n\nconst transform = pipe(double, addThree, toString);\nconsole.log(transform(5));',
      expectedOutput: 'Result: 13',
      hints: ['reduce で関数を左から順に適用', '(x) => fns.reduce((v, fn) => fn(v), x)'],
      explanation: 'pipe は関数合成の基本パターンです。5 → double(5)=10 → addThree(10)=13 → toString(13)="Result: 13"。単一責任の小さな関数を組み合わせることで、テストしやすく可読性の高いコードが書けます（リーダブルコードの原則）。',
    },
  ],
};

export default course;
